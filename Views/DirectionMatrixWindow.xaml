<Window x:Class="StrategyViewer.Views.DirectionMatrixWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StrategyViewer.Views"
        Title="品种多空矩阵" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#1e1e2e"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#585b70"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#313244"/>
                                <Setter Property="Foreground" Value="#6c7086"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HeaderCell" TargetType="Border">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="BorderBrush" Value="#313244"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="DateCell" TargetType="Border">
            <Setter Property="Background" Value="#3b3d4f"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="EmptyCell" TargetType="Border">
            <Setter Property="Background" Value="#1e1e2e"/>
            <Setter Property="BorderBrush" Value="#313244"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="LongCell" TargetType="Border">
            <Setter Property="Background" Value="#f38ba8"/>
            <Setter Property="BorderBrush" Value="#f38ba8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="ShortCell" TargetType="Border">
            <Setter Property="Background" Value="#a6e3a1"/>
            <Setter Property="BorderBrush" Value="#a6e3a1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>

        <!-- 策略详情弹窗样式 -->
        <Style x:Key="StrategyPopupStyle" TargetType="Popup">
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Placement" Value="Mouse"/>
            <Setter Property="PlacementTarget" Value="{Binding RelativeSource={RelativeSource AncestorType=Border}}"/>
            <Setter Property="StaysOpen" Value="False"/>
            <Setter Property="IsOpen" Value="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}, Mode=OneWay}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="品种多空矩阵" FontSize="20" FontWeight="Bold" Foreground="#89b4fa" VerticalAlignment="Center"/>

            <!-- 图例 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="30,0,0,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <Border Width="20" Height="20" Background="#f38ba8" CornerRadius="4"/>
                    <TextBlock Text="多" Foreground="#f38ba8" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <Border Width="20" Height="20" Background="#a6e3a1" CornerRadius="4"/>
                    <TextBlock Text="空" Foreground="#a6e3a1" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="最近30天策略品种多空变化 | 日期从近到远" Foreground="#6c7086" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="2" Content="{Binding CollapseButtonText}"
                    Command="{Binding ToggleCollapseCommand}"
                    Padding="12,6" FontSize="12"/>
        </Grid>

        <!-- 矩阵表格 -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="MatrixGrid">
                <!-- 动态内容由代码生成 -->
            </Grid>
        </ScrollViewer>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#313244" Padding="12,8" Margin="0,16,0,0" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusMessage}" Foreground="#6c7086" VerticalAlignment="Center"/>

                <ProgressBar Grid.Column="1" IsIndeterminate="{Binding IsLoading}" Width="100" Height="4"
                            Margin="0,0,16,0"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                            Background="#45475a" Foreground="#89b4fa"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="SaveImageButton" Content="保存图片"
                            Click="SaveImage_Click"
                            Padding="12,6" FontSize="12" Margin="0,0,8,0"/>
                    <Button x:Name="SendFeishuButton" Content="发送到飞书"
                            Click="SendFeishu_Click"
                            Padding="12,6" FontSize="12" Margin="0,0,8,0"/>
                    <Button x:Name="SendTelegramButton" Content="发送到 Telegram"
                            Click="SendTelegram_Click"
                            Padding="12,6" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4" Background="#80000000" CornerRadius="8"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="加载中..." FontSize="18" Foreground="#cdd6f4"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
